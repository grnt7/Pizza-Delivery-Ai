# Monorepo: use package-local env instead of duplicating secrets here.
#
# Convex + Clerk secrets  → backend/.env.local (and Convex dashboard env vars).
# Expo public keys + URL   → apps/frontend/.env (EXPO_PUBLIC_* only).
