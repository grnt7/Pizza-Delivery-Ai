{
  "name": "@pinnochios/convex-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "lint": "tsc --noEmit -p convex/tsconfig.json"
  },
  "dependencies": {
    "convex": "^1.35.1",
    "stripe": "^18.0.0",
    "svix": "^1.94.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
